/*Fazer uma matriz com 3 linhas e 4 colunas (3x4) e depois inverter o valor das colunas.
Make a matrix with 3 rows and 4 columns (3x4) and then inverter the value of the columns.*/

#include<stdio.h>
#include<stdlib.h>

int main(){

int matriz[3][4],i,j;
  printf ("\nDigite os valores para os elementos da matriz\n\n");

  for(i=0;i<3;i++){
    for(j=0;j<4;j++){
       printf ("\nElemento[%d][%d]=",i,j);
       scanf ("%d", &matriz[i][j]);
    }
  }
   printf("\n\nSAIDA DE DADOS\n\n");
 
   for(i=2;i>=0;i--){
    	for(j=0;j<4;j++){
       	printf("\nInversao de COLUNA: [%d][%d]=%d\n",i,j,matriz[i][j]);
    	}
  }
  system("pause");
}
